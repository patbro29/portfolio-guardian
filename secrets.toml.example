# Copy this file to .streamlit/secrets.toml and fill in your key.
# NEVER commit secrets.toml to git. .gitignore already excludes it.

# FREE — get this at https://aistudio.google.com/apikey (default provider)
GEMINI_API_KEY = "AIza..."

# OPTIONAL — only needed if you set LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY = "sk-ant-api03-..."
